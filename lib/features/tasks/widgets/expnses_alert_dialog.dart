import 'package:flutter/material.dart';

class ExpensesAlertDialog extends StatelessWidget {
  const ExpensesAlertDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
